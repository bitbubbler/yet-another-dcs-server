import { CoalitionWorldState } from './types'

export class CoalitionAgent {
  /**
   * Public world state that is used to share world knowledge
   * between unit agents.
   */
  // public worldState: CoalitionWorldState
}
